/*

This is your global styles file. Selectors and rules you define
here will be applied throughout your app. However, these rules
have the lowest priority of any style settings.

For more information, see the "Style Priorities" section of
http://docs.appcelerator.com/platform/latest/#!/guide/Alloy_Styles_and_Themes

For example, the following would apply to all labels, windows,
and text fields (depending on platform) in your app unless you
overrode the settings with other TSS, XML, or JS settings:

'Label[platform=android,windows]': {
	color: '#000' // all platforms except Android and Windows default to black
}

'Window': {
	backgroundColor: '#fff' // white background instead of default transparent or black
}

'TextField[platform=android]': {
	height: Ti.UI.SIZE
}

*/

//TopBar
".noTopBar[if=Alloy.Globals.isIOS]" : {
    top: 20
}

// Sizes
"Window": {
    backgroundColor: 'white',
}
"Window[if=Alloy.Globals.isIOS]": {
    width: Ti.Platform.displayCaps.platformWidth,
    statusBarStyle: Ti.UI.iOS.StatusBar.LIGHT_CONTENT
}
"Window[if=Alloy.Globals.isAndroid]": {
    animated: false
}

"Label":{
    font :{
		fontFamily: Alloy.CFG.fonts.Simple
	}
}

".wSize":{
	width: Ti.UI.SIZE,
}

".hSize":{
	height: Ti.UI.SIZE,
}

//positions
".t0":{
	top: 0
}

".l0":{
	left: 0
}

".b0":{
	bottom: 0
}

".r0":{
	right: 0
}

//layouts
".vLayout":{
    layout: 'vertical'
}

".hLayout":{
    layout: 'horizontal'
}

//Separators
".hSeparator":{
	height: "1",
	width: Ti.UI.FILL,
}

".vSeparator":{
	width: "1",
	height: Ti.UI.FILL
}

// Background colors
".bgBlue":{
	backgroundColor: "blue"
}

".bgWhite":{
	backgroundColor: "white"
}

".bgGold":{
	backgroundColor: "yellow"
}

// Text colors
".txtWhite":{
	color: "white"
}

".txtBlack":{
	color: "black"
}

".txtGold":{
	color: "yellow"
}

".txtBlue":{
	color: "blue"
}

// Buttons

".primaryButton":{
	backgroundColor: Alloy.CFG.colors.primaryColor
}

".disabledButton":{
	backgroundColor: Alloy.CFG.colors.disabled
}

".greenButton":{
	backgroundColor: Alloy.CFG.colors.green
}


".purpleButton":{
	backgroundColor: Alloy.CFG.colors.purple
}

".lightgrey":{
	backgroundColor: Alloy.CFG.colors.lightGrey
}

// Fonts
".txtBlack":{
	color: Alloy.CFG.colors.Fonts.black
}

".txtPrimary":{
	color: Alloy.CFG.colors.fonts.primary
}

".txtPlaceholder":{
	color: Alloy.CFG.colors.fonts.placeholder
}

".txtYellow":{
	color: Alloy.CFG.colors.fonts.yellow
}

".txtRed":{
	color: Alloy.CFG.colors.fonts.red
}
".blackFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.Simple
	}
}
".italicFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.Italic
	}
}
".boldFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.Bold
	}
}
".boldItalicFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.BItalic
	}
}
".lightFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.Light
	}
}
".lightItalicFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.LightItalic
	}
}
".RegularFont":{
	font :{
		fontFamily: Alloy.CFG.fonts.Regular
	}
}

".CommunButton" : {
    color: "white",
    height: 46,
    borderRadius: 5,
    left: "10%",
    right: "10%",
    textAlign: Ti.UI.TEXT_ALIGNMENT_CENTER,
    font: {
        fontSize: 16,
        fontWeight: "semibold"
    },

}


".navBar":{
	backgroundColor: Alloy.CFG.colors.primaryColor
}

".imageInsideButton":{
    left: 15
}



".lebelInsideButton":{
    left: 67,
    color: "#ffffff"
}

".iosMarginTop[if=Alloy.Globals.isIOS]" : {
    top: 20
}
".iOSwindowTop[if=Alloy.Globals.isIOS]":{
    top: 25
}
"Window[platform=ios]":{
  navBarHidden: "true",
}

".androidNoActionBar[if=Alloy.Globals.isAndroid]": {
    theme: "Theme.AppCompat.NoTitleBar"
}

"Window[platform=android]":{
    theme : 'Theme.AppCompat.NoTitleBar',
    top: 25
}
